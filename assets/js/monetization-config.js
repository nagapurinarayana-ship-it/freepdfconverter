/*
 * AdSense activation
 * ------------------
 * Keep these values blank until the site has an approved AdSense account.
 * Then replace them with the real publisher and ad-unit IDs from AdSense.
 * Never use sample/fake IDs in production.
 */
window.FreePDFMonetization = Object.freeze({
  adsenseClient: "",
  slots: Object.freeze({
    top: "",
    content: "",
    footer: ""
  })
});
